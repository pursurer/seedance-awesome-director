# Video QA / 成片核对

This record covers one Dreamina generation and its local 10-second edit. It uses a [20-frame contact sheet](edited-10s-contact-sheet.jpg) sampled every 0.5 second plus full-resolution frames around the two door actions. Timestamps below refer to the edited clip; add 2.000 seconds for the source clip. This sampling does not certify every frame or the audio track.

| Edited time | Observed result |
| --- | --- |
| 0.0–1.5 s | The clip begins with the woman still on the street, holding one blue envelope. The original 0–2-second approach was intentionally removed. |
| 1.5–3.5 s | From the street side, her hand reaches and visibly grips the brass bar on the green door. The door opens toward the bookstore before she crosses the threshold. |
| 4.0–6.5 s | She places the single blue envelope on the oak counter and releases it. The envelope remains there. |
| 7.5–9.5 s | Empty-handed, she grips the interior handle and leaves through the open doorway. The same blue envelope remains visible on the counter. |

The sampled images show the requested shot order, character look, envelope state, and inward door swing at the key actions. They do not rule out brief hand, hinge, or body artifacts between sampled frames. The original prompt asks for no watermark, but a small `AI` marker is visible at the upper left of the output. Audio content was not independently reviewed. Thus this pilot is **not** evidence of flawless output or improvement over a matched baseline.

| QA dimension | Provisional status |
| --- | --- |
| Shot sequence | Observed in order; the edited clip omits only the requested first 2 seconds |
| Identity and envelope state | Consistent at sampled points |
| Motion and spatial continuity | Door actions visible at sampled points; full-frame continuity not certified |
| Audio | Not reviewed |
| Output hygiene | 854×480 and exactly 10.000 s in the edited file; `AI` marker conflicts with the no-watermark prompt |

No second generation or repair was attempted after this result. The edited preview was selected for presentation with these limitations documented.
